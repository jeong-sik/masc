### Internal

- Terminal-Bench tool counts treat a call row with neither `disposition` nor
  `wire_outcome` as unsettled, not as a broken row, so such a row no longer
  turns the trial's tool counts into null (#38721).
