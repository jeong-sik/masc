---
description: Subset-rewrite advice templates — what the caller types instead when the typed argv form cannot carry the shell they wrote
category: tool
operator_surface: fragment
---
### move_to_field (vars: field, because)
use the {{field}} field: {{because}}

### call_this_instead (vars: call, because)
call {{call}} instead: {{because}}

### spell_it_as (vars: spelling, because)
write it as [{{spelling}}]: {{because}}
