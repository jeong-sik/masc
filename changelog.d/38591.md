### Internal

- The edited-test runner now obeys the custom-bound hook only under `--self-test`, so an inherited environment variable cannot hand a second suite the long timeout reserved for the keyboard walk (#38591).
