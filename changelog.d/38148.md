### Fixed

- The TUI Overview draws an Attention item about a Keeper that the Team block lists once, on that Keeper's Team row, instead of also in the Attention panel. One stopped Keeper used to take two lines and read as two problems. The Attention panel draws the remaining items and says how many moved (`(7 on Team rows below)`); when the screen is too short for the Team block, the panel draws every item (#38148).
