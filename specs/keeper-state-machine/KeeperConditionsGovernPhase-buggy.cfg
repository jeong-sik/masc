\* Keeper conditions-govern-phase — bug model
\* Buggy: WF on Transition is dropped, so the FSM may stutter
\* indefinitely in (handoff_needed=TRUE, phase="Running").
\* HandoffEventuallyAcknowledged MUST be violated — TLC should report
\* a temporal counter-example.  If it still passes, the spec is too
\* weak and must be strengthened before relying on the divergent-
\* conditions UI (PR 6b).

SPECIFICATION SpecBuggy

INVARIANTS
    TypeOK

PROPERTIES
    HandoffEventuallyAcknowledged

CHECK_DEADLOCK
    FALSE
