# Autonomous Chat execution evidence

The installed aad94 Chat history projected turn428 as two Edit activity summaries without execution IDs. The tool ledger retained both canonical executions and before/after originals. Expanding 183 real UI controls still could not show the snapshot-bearing execution row. The selected observations here preserve the missing Chat identity and retained ledger identity; full browser failure receipts/screenshots are in the collaboration-resume evidence worktree.

This change carries each raw tool start sequence through AGENT_CORE trajectory, pairs actual invocation coordinates, and uses the selected TurnRecord execution IDs to query authoritative ledger rows. The derived index advances once for the batch and reads original rows across the whole retained ledger. Duplicate IDs and duplicate raw starts remain ambiguous. The projection checks Keeper, session, Keeper turn, invocation turn/index and raw tool identity, then exposes only the canonical execution ID. Reasoning text, raw inputs/results and provider tool-use IDs remain outside this projection. The Dashboard treats execution-linked autonomous work as inspectable tool evidence.

Validation: OCaml parse-only checks passed; the targeted Vitest test opening an autonomous Edit and selecting its stored originals passed (1 passed, 165 skipped). This is a synthetic component test, not installed browser acceptance. Independent review identified overlapping provider-ID pairing and a missing test record field; both were repaired. A follow-up review also required rejecting duplicate raw invocation starts and a test that isolates that ambiguity; both are included. Native tests, installation and real browser acceptance are still required. The latest-200 tool-output API remains a separate older-history hydration gap even though the server execution-ID projection uses the whole retained ledger.
