### Removed

- The model inference metrics no longer carry a `provider` field. The parser never filled it, so every model stat and recent entry had `provider: null` in the JSON and the Dashboard drew nothing from it (#38604).
