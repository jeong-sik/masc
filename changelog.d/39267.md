### Fixed

- The preset list reads each preset the way the detail does, through
  `Prompt_preset.load`, so a preset listed in Config › presets always opens.
  One whose files no longer read (an overrides file of an older schema) is
  listed as unreadable with the reason loading gives, rather than shown with
  a summary that then fails to open (#39267).
