{
  "id": "l2-py-wordcount",
  "level": "L2",
  "lang": "python",
  "timeout_sec": 900,
  "verify": "verify.sh",
  "prompt": "check.sh fails because wordcount.py's word_counts(text) is not implemented. Implement it so the check passes: split on whitespace, lowercase each token, and return a dict mapping each token to how many times it appears. Do not modify check.sh. Reply DONE once `bash check.sh` prints PASS.",
  "description": "Small feature: implement word_counts(text) in wordcount.py to satisfy the cases in check.sh."
}
