### Fixed

- A provider usage counter that went backwards or reset no longer freezes per-turn usage accounting. `Counter_regressed` used to keep the stale high-water baseline, so every following turn answered `delta = None` until the new counter climbed back past the old mark; the regressed sample now becomes the next baseline, so exactly that one turn is lost and the next turn resumes an exact delta (#38646, fixes #32463).
