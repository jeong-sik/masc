\* Carried over from TaskLifecycle.tla: a verdict read against a replaced
\* submission must not accept the Task.

SPECIFICATION SpecBugSupersededVerdict

CONSTANTS
    Tasks = {t1, t2}
    Agents = {a1, a2}
    MaxSubmissions = 2
    NoOne = NoOne
    Operator = Operator

INVARIANTS
    AcceptedRequiresLiveConfirmation

CHECK_DEADLOCK
    FALSE
