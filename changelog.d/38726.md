### Fixed

- `masc_dos_load` takes `mouse` (default false). With it the DOS guest's
  INT 33h reports a mouse, so a program that checks at start sees the one
  `masc_dos_click` moves. A click on a machine loaded without it is refused
  with a message naming `mouse=true`, instead of succeeding while the guest
  believed there was no mouse (#38726).
