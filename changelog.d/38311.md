### Fixed

- The Config / Runtime authority line draws the config file it is about. The line is about one file, and that file's path was its last reading, behind the probe tally and the whole fleet's keeper totals; what stood in front was the name `runtime.toml` spelled in the binary rather than the one the server answered with. Live the line ran to 189 cells in a pane 145 wide, so neither the path nor the `111 probe-only` count drew, and a reader of a store at `/Users/<user>/me/.masc/config` saw only the spelled name. The readings are now ordered by how much they belong to this screen -- the file, what the probe made of it, and last the fleet totals, which are the Overview's subject and are what the right edge takes. A reading the screen has not got is now absent rather than an empty string carrying its own separator (#38311).
