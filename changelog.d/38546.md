### Fixed

- A microVM or remote_ssh Keeper's Read and search tools accept a cwd that
  exists only on the endpoint. They asked the host, which answered "no
  repository is materialized" for a checkout in the guest; Execute already
  asked the endpoint (#38546).
