### Changed

- The runtime failover order is named `Runtime Candidate Order` everywhere on the screen: the chat heading and the runtime picker header were the last two strings still saying `FAILOVER`, and a `media_failover` line the rename itself had added called the vision fleet's call order by the new name, which the glossary defines as a different mechanism. Both are now named what they are (#37918).
