### Fixed

- `masc_operator_judgment_write` records a judgment under the caller's authenticated name. The `keeper_name` body field, which let a caller write a judgment as another keeper, is removed from the tool (#38300).
