\* Cancellation token boundary — clean model.
\* CleanCancel pairs the reason write with the cancelled CAS atomically;
\* ReentryNoOp is the no-op path for already-cancelled tokens.

SPECIFICATION Spec

INVARIANTS
    TypeOK
    ReasonBeforeCancelled
    CallbacksFiredAtMostOnce

CHECK_DEADLOCK
    FALSE
