### Fixed

- `/api/v1/dashboard/keeper-costs` counts the turn rows the window may hold
  but whose `ts_unix` or `latency_ms` it cannot read as
  `metrics_read.unread_turn_rows`, and lists Keepers whose meta it cannot
  read or decode under `keepers_unread`, in the operator snapshot's shape.
  Both used to be dropped without a trace, leaving a floor that read as an
  exact sum (#38728).
