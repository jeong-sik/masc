### Fixed

- The Keeper continuity validation harness no longer runs a handoff phase that no run could pass; a live run whose continuity holds can now classify as PASS, and an unknown `TARGET_PHASES` name stops the run with exit 2 (#38455).
