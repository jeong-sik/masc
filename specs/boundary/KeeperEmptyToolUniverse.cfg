\* Clean spec — post-Phase-B PR-4 production. NoEmptySurfaceWithoutFeedback
\* and EmptySurfaceImpliesFeedback both hold: the EmptySilentSkip action
\* is unreachable from this Next relation, so silentSkip stays FALSE
\* for every keeper and every transition to surfaceEmpty=TRUE coincides
\* with feedbackLogged=TRUE.  TLC must report "no error".

SPECIFICATION Spec

CONSTANTS
    Keepers = {k1, k2}

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
