### Changed

- The Overview Team block's spend reads keeper-costs' `unread_turn_rows`: a
  turn row whose time or latency the server could not read may have been a
  turn in the window, so that Keeper's sums are drawn as floors (#38813).
