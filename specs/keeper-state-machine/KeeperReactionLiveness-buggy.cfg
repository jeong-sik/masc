\* KeeperReactionLiveness — buggy model (BugSilentStimulusDrop)
\* Expected: TLC exits 12 or 13 (liveness violation on BoardEnqueueLeadsToReceipt)
\*
\* The buggy spec removes the receipt/terminal-reason guarantees (IssueReceipt
\* and IssueTerminalReason are no longer weakly fair) and replaces them with
\* WF(BugSilentStimulusDrop), modelling the silent-drop failure mode.
\* BoardEnqueueLeadsToReceipt MUST be violated: the stimulus cycles through
\*   none -> queued -> in_turn -> none (BugSilentStimulusDrop)
\* indefinitely without ever reaching receipt_issued or terminal_reason.

SPECIFICATION SpecBuggy

CHECK_DEADLOCK
    FALSE

INVARIANTS
    TypeOK

PROPERTIES
    BoardEnqueueLeadsToReceipt
