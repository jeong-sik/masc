### Fixed

- A failed keeper cycle no longer names a runtime as the one that ran when the agent_core pipeline refused to send its request (`Attempt_rejected`, e.g. a missing output ceiling); no provider saw that request, so the attempt is recorded as rejected before dispatch (#38562).
