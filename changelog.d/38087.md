### Upgrade notes

- A schedule call whose `*_by_id` argument names someone other than the caller now fails with `actor_mismatch` instead of being silently overwritten, and the literal `"operator"` default for `requested_by` is gone. Omit the actor-id fields or pass your own name (#38087).

### Fixed

- A schedule call that names a different actor is refused with `actor_mismatch` (#38087).
