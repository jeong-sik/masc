(include_subdirs no)

(library
 (name masc_time_codec)
 (public_name masc.time_codec)
 (wrapped false)
 (modules time_codec)
 (flags
  (:standard -w +4 -w +33))
 ; [unix] is for [Unix.gmtime] in the writer. Every library that already
 ; depends on this one also depends on unix, so no consumer's dependency
 ; closure changes.
 (libraries ptime unix))
