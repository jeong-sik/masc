### Fixed

- The chat screen now reports the tokens a turn in flight has spent: the input, output and cache counters the provider reports mid-stream appear beside the runtime on the keeper header, and a counter the provider did not report is left out rather than drawn as zero (#38498).
