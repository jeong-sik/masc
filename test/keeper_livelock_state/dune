; Standalone alcotest for Keeper_livelock_state. The unit module
; itself is stdlib-only, so this test executable links in seconds
; without dragging the main masc library.

(tests
 (names test_keeper_livelock_state)
 (libraries alcotest masc.keeper_runtime))
