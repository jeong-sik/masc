Verification against `/Users/dancer/me/.tmp/fusion-spec.html`:

- The report's main UI gap (no renderer for `meta.panel` / `meta.judge`) is **partially addressed on current `main`**:
  - `post-detail.ts` renders `<FusionBoardEvidence>`.
  - A standalone `FusionSurface` exists.
- The remaining structural issue is the duplicated parser between `board/fusion-evidence.ts` and `fusion-surface.ts` described in this issue.
- Addressed in draft PR #21965 by moving panel/judge/usage normalization into `lib/fusion-meta.ts` and having both surfaces import it.

Checks run in the PR branch:
- `pnpm typecheck` passed
- focused vitest suites for `post-detail`, `fusion-surface`, and `chat/primitives` passed
