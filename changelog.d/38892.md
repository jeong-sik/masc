### Added

- Dashboard Settings edits runtime candidate lanes (`[runtime.lanes.<id>]`):
  reorder, add or remove a candidate, rename, delete and create a lane. An
  edit applies to the order the file declares, so a candidate the catalog
  dropped is kept. The keeper assignment picker offers declared lanes
  (#38892).
