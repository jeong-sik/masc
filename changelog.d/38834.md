### Fixed

- Keeper metadata in the earlier v1 format now loads without resetting
  counters, pause state, or task binding; new writes remain v2. A
  schema-invalid JSON metadata file still reports the Keeper, path, and reason
  before fail-open treats it as absent (#38834).
