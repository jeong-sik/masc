### Internal

- `Exact_output.success` now carries `usage : Types.api_usage option`, the
  token usage the provider's response body reported, taken from the same
  wire parse the output comes from. `None` means the body reported no usage;
  it is never a zero-filled count. Official-client CLI slots produce no
  `success` and report no usage (#38667).
