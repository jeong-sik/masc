### Fixed

- The Fusion run detail and the Lanes run detail now keep the scroll position
  on their footer. It was spelled onto the end of the key hints, so the
  fitter gave it up first, and at sixty and eighty columns it was never drawn
  (#38820).
