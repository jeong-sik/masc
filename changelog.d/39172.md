### Fixed

- The TUI tab strip keeps its Approvals entry while the durable Gate queue is
  unread, failed to load, or unavailable on the server; the strip and the
  Overview "Approvals:" row now decide "every approval list was read" with one
  predicate (#39172).
