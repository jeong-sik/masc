\* Clean spec — post-RFC-0313 pacing discipline.
\* Safety (TypeOK + NoFailureDrivenExistenceChange + PacingBounded) must pass.

SPECIFICATION Spec

CONSTANTS
    Runtimes = {r1}
    MaxTime = 3
    MaxBackoff = 2

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE