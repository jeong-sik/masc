### Internal

- `scripts/review/approve-guard.sh` renames `--replace-own-cr` to `--replaces <review id>` and the posted footer reads `replaces review <id>`; the selftest asserts the new footer. (#38974)
