### Fixed

- The TUI's Task Review and Verdicts list panes say when each row was sent.
  Both lists hold a task once per submission and each row drew its task id
  and nothing else: measured on the live history, 200 Task Review rows carry
  113 distinct ids, and seven of the rows are one task submitted by one
  agent with no stated intent, parted only by their clocks (#38492).
