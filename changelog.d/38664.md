### Internal

- Browser lane names (`live`, `automation`) are decoded in one place,
  `Browser_lane.Lane_name`; every reader matches on the variant without a
  catch-all, so a new lane fails the build at each reader instead of being
  refused at runtime by the one that was missed (#38664).
