### Changed

- The deploy preflight and boot read one durable-store list,
  `Keeper_durable_store`, whose type carries each store's boot policy
  (`refuse_boot`, `degrade_typed`, `preflight_only`); boot behaviour is
  unchanged, and the preflight now also refuses a `goals.json` this build
  cannot decode (#39226).
