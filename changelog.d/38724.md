### Fixed

- `dune build @check` no longer stops on a partial-match warning in
  `test_server_skill_editor.ml`. The case that recreates a Skill under a
  freshly deleted package id now names the `Created_but_shadowed` outcome
  (#38724).
