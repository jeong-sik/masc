### Fixed

- A task's description now draws the line breaks it was written with. The detail pane printed every break as the four characters `\x0A` inside one wrapped paragraph, so a description written as a list or as separate paragraphs arrived as one run-on line. A break in the middle keeps its blank row, a run of breaks at either end is dropped so no blank row takes the label's place, and a CRLF break leaves no carriage return behind on the row (#38030).
