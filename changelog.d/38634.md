### Added

- Add bounded retention and rotation to multimodal vision artifact stores (`default_max_entries = 500`, `default_max_bytes = 20 MB`) with automatic eviction of older canonical files on writes to bound disk consumption (#38634).
