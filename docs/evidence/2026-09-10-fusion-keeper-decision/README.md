# Actual Keeper Fusion decision and readback

An isolated Kimi Keeper chose Fusion for the supplied decision problem, received a real Kimi + GLM panel and GLM judge/refinement result, and wrote its choice in prose. A separately labelled follow-up explicitly requested `masc_fusion_decision`; the Keeper then recorded its own `adopted` choice and reason. The same typed decision was independently read through Task history and Fusion detail and rendered in the exact CI-built Dashboard. This does **not** establish spontaneous use of the new structured recording tool: it was absent from the initial turns' loaded schemas and was used after the explicit follow-up.

The runtime and preview source are `81066b198c7e04f9bef03d5f2af8882113fffb30`. Runtime CI [34418620023](https://github.com/jeong-sik/masc/actions/runs/34418620023) supplied artifact `10130164313`; preview CI [34418566609](https://github.com/jeong-sik/masc/actions/runs/34418566609) supplied artifact `10130052728`. The downloaded runtime manifest and live health agreed; executable SHA-256 was `68507dd89a2614b1cf9c4766309977f96a3fb3d41875c8b98ba11695a0d91ac4`. Every preview file was checked against its provenance manifest. No local build or production deployment was performed.

## Observed sequence

- Initial operation `kmsg-96fbfa3ff1f7b5a11207b554a6bd0a01` failed before a provider call because the harness removed the referenced Kimi credential environment variable. Its failure is retained. After explicitly repairing environment forwarding and restarting only the owned server, operation `kmsg-3b0d820c21168eca6bd64774fb15f02b` succeeded.
- The initial message named no Fusion tool and did not require a panel. This remains a targeted evaluation with a Keeper named `fusion-evidence-review`, an explicitly important decision, and the existing shared Keeper instructions; it is not a measurement of unprompted fleet-wide behavior.
- The Keeper claimed `task-001`, read Goal `g-fusion-evidence-review-20260910`, and invoked Fusion run `kmsg-cd179ef2b1978a75c96df4c82478c260` with actual `task_id`, `goal_id`, `decision_context`, and `topology=refine`. Both panel responses succeeded. The accepted obligation's complete Task contract, Goal criterion, original question, and rendered model prompt exactly matched the published Board metadata at `p-d3729e4249c8b0fd9485ff28e75fecee`.
- The completion wake led the Keeper to select evidence-link repair, submit the evaluation Task for verification, and write prose Board post `p-15c1796c44f8b385636be20d5dd3f348`. The Task was subsequently **rejected**, including because the verifier could not access the cited source artifacts. This evidence does not claim Task or Goal completion.
- Explicit structured-recording follow-up `kmsg-12af2a401bea7ba73e1e7319a53f5200` succeeded. Decision `08e78fb51a43b06ccaad6ccf13fccd7c57e75519ccacbe904f6244bc0648f06e` binds that same Fusion run, Task, Goal, Keeper and actual turn `trace-1788998630329-00000#5`. The Keeper chose `adopted` and wrote the choice and reason; the operator did not insert a decision event. An earlier follow-up transport attempt was refused after MCP session expiry; its pending intent and independent no-new-operation reconciliation are retained.

## Evidence and limits

`scenario-input.json` contains exact Git bytes and hashes from evidence commit `97b6477f29`. These are **historical** observations about different artifacts, not the current PDF or original Task state. The question, Goal, Task contract, and both operator messages are included. Provider credentials, raw thinking, and the private corpus are excluded. The panel and judge share the GLM model; distinct calls do not prove independence of training or bias. Model recommendations are evidence of the deliberation, not independently verified facts: for example, the synthesis's inventory mentions `42d0b96d`, which the supplied source identifies as an execution-output digest, not a PDF digest.

`actual-tool-ledger.json` retains selected model tool inputs/results and metadata. `accepted-fusion-obligation.json`, `browser-receipt.json`, and `operation-observation.json` connect acceptance, publication, typed recording, independent API readback, and terminal operation outcomes. `provider-tool-presence.json` contains only selected schema-presence observations: `masc_run_plan` was already loaded even though name search returned “not in the list”; the complete provider input is not published. Unnecessary Execute attempts were parked at Gate and were not approved by this harness. Their presence is not hidden or counted as completed work.

The desktop screenshot visibly shows the actual adopted choice and reason in Task history. The 390px screenshot preserves a **real mobile defect**: the dialog is 343px wide but its contents span 503px, clipping a long SHA. A separate repair is required. Browser writes, WebSockets, and off-origin requests were blocked. The header's missing-dashboard-artifact warning is real: the isolated server has no installed Dashboard bundle; the browser receives separately downloaded, manifest-verified CI preview assets while API reads go to the actual authenticated server. No API response was replaced with a fixture.

`sha256.json` binds the saved evidence files. The three scripts under `scripts/` prepare private configuration, operate the exact isolated binary, and verify the live preview. They do not edit Keeper journals or manufacture tool calls.
