### Fixed

- A DOS guest that stops on an instruction the emulator does not implement
  no longer leaves the step count behind the instructions that ran before it.
  Those instructions may have repainted the screen, and a watcher that keys
  the picture on the step count kept showing the frame from before (#38715).
