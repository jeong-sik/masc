### Fixed

- A vision candidate arm's end is logged as a lifecycle event, like its start.
  It was logged as a tool call by keeper `system`, so 4,312 rows a week (342
  failures) counted in tool quality alongside the parent
  `keeper_analyze_image` call (#38551).
