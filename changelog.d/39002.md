### Fixed

- The Board read footer drew `z:wide` in both of that key's states, so a wide
  detail offered the operator the screen they were already on. The footer read
  the split, which is "wide enough and not wide mode" and so cannot tell a
  narrow terminal from a hidden list; the wide-mode flag now travels on its own
  and the label follows it -- "wide" from a split detail, "list" from a wide one
  (#39002).
