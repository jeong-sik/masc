### Added

- TUI: after a Keeper pause or shutdown is accepted, a Keeper that still holds channel bindings gets a one-line footer offer naming them; `U` removes them with the same conditional unbind as `U` `U`, and any other key keeps them. The offer takes only a key read after it was drawn, and only for the selected Keeper (#38191).
