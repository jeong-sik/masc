### Fixed

- The chat view now says why the provider stopped writing a turn, next to the tokens it spent: `stopped: max_tokens`, `stopped: end_turn`, and every other reason the provider reports. A reply cut off because the model ran out of room used to look exactly like a finished one on this screen, while the dashboard showed the reason all along (#38508).
