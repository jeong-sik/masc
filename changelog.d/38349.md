### Performance

- The Activity screen sizes its KEEPER and EVENT columns from the two words it measures instead of building a full row — with its detail sentence — for every event the feed still holds, on every frame (#38349).
