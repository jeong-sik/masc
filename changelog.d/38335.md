### Fixed

- A settled Antigravity keeper whose system prompt or history changed since its last turn now starts a fresh conversation instead of refusing every cycle with `canonical_context_changed`. The refusal could not be cleared: the session stayed `Settled`, so the resolve endpoint had nothing to act on. A settled session with no acknowledged snapshot takes the same path. An unfinished Gate continuation still refuses, and the fresh start is logged with the keeper and the reason (#38335).
