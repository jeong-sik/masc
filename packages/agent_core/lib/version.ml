(** Single source of truth for the embedded agent-core version string.
    All other modules reference this instead of hardcoding. *)

let version = "0.231.14" (* x-release-please-version *)
let name = "agent_core"
