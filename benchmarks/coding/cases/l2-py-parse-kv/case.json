{
  "id": "l2-py-parse-kv",
  "level": "L2",
  "lang": "python",
  "timeout_sec": 900,
  "verify": "verify.sh",
  "prompt": "check.sh fails because parsekv.py's parse_kv(text) is not implemented. Implement it so the check passes: parse `key=value` lines into a dict, skip blank lines, and let a later duplicate key win. Do not modify check.sh. Reply DONE once `bash check.sh` prints PASS.",
  "description": "Small feature: implement parse_kv(text) in parsekv.py.",
  "build": "build.sh"
}
