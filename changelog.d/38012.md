### Fixed

- The Team screen's fleet row counted task owners it could not read as if they were not there: the scan reports what it could not read (the backlog, or a Keeper whose profile did not load), but only a backlog failure moved the fleet status off `ok`, so a Keeper whose profile failed left its tasks out of the count with nothing on the row saying so. The entry now carries its own shortfall -- `task owner without fiber 0 (2 sources unread)` -- and with nothing found and nothing missed the entry stays off the row as it did before (#38012).
