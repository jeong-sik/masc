; Isolated alcotest for the comment-preserving TOML line editor (RFC-0306 3.2).
; Links otoml too, so a produced file is judged by the loader that will read it
; rather than by counting lines.

(test
 (name test_toml_line_editor)
 (libraries alcotest otoml masc.toml_line_editor))
