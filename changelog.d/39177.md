### Fixed

- The TUI Overview Team block no longer draws every alive Keeper as idle with
  "no open task" when the backlog read failed or has not happened yet; those
  Keepers show "tasks unread" and the title counts them as "alive (tasks
  unread)" (#39177).
