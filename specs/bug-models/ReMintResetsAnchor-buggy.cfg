\* Buggy model: ReMintResetsAnchor sets anchor := now on every re-extraction
\* (the pre-P6 "re-observing IS re-verification" rule for a volatile claim).
\* Expected: NoUnverifiedVolatileClaimSurvivesBeyondHorizon VIOLATED — a never-
\* grounded claim stays surfaced as live past the grounding horizon.
SPECIFICATION SpecBuggy
INVARIANT TypeOK
INVARIANT NoUnverifiedVolatileClaimSurvivesBeyondHorizon
CONSTANTS
    Horizon = 2
    MaxTime = 4
CHECK_DEADLOCK FALSE
