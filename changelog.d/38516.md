### Fixed

- A stored interval schedule and a newly requested one follow one rule: the
  60-second floor that applied only on create and modify is gone, so a
  schedule written before the floor no longer keeps an interval that the same
  declaration is refused, and editing its payload is no longer refused. Every
  path accepts any positive `interval_sec`; the schedule runner tick bounds how
  often it fires (#38516).
