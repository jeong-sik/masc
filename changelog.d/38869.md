### Fixed

- `masc.opam.locked` pins `cohttp-eio` to the fork carrying the SSE body fix,
  so builds that replay the lock file (the keeper sandbox image) get the fix
  too (#38869).

### Internal

- `/ocaml-msx.opam`, generated by the first build, is kept out of commits, and
  the dependency hints in `scripts/dune-local.sh` and
  `scripts/coverage_percent.sh` install from `./masc.opam` so that file cannot
  shadow the `ocaml-msx` git pin; the opam pin script's repair hint covers the
  local switch the README creates (#38869).
