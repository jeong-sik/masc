\* RFC-0313 keeper pacing — clean model
\* Clean: Safety (TypeOK + NoFailureDrivenExistenceChange + PacingBounded)
\* must hold: failures only widen per-runtime revisit deadlines.

SPECIFICATION Spec

CONSTANTS
    Runtimes = {r1, r2}
    MaxTime = 4
    MaxBackoff = 2

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
