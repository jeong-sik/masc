name = "masc_msx_screen"
description = """
Read the current MSX frame without advancing time: mode, PC, text, tiles and sprites. Keeper calls return a PNG artifact and dimensions from the same frame; use keeper_analyze_image to read it. Bitmap screen_text is not game text; tile numbers are game-specific."""
additional_properties = false

defer_loading = true
