### Fixed

- The Team screen's fleet row now says when its owner scan came up short, as in `task owner without fiber 0 (2 sources unread)`. A Keeper whose profile failed to load used to drop out of that count with nothing on the row saying so, so a partial scan drew the same row as a complete one. With nothing found and nothing missed the entry stays off the row, as it did before (#38012).
