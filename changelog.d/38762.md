### Fixed

- `dune build @check` no longer stops on `test_exact_output_flow.ml`: one `catalog_entry` call still passed `~body_timeout_s:0.5` after #38573 made the field `float option`, so main turned red the moment that commit landed. The call now passes `(Some 0.5)` like every other call site in the same file (#38762).
