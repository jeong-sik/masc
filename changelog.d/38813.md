### Changed

- The Overview Team block's spend reads keeper-costs' `unread_turn_rows`
  and `keepers_unread`: a Keeper with unreadable turn rows gets a floor, and
  a Keeper whose meta the server could not read is drawn unknown with the
  server's reason, which makes the team total a floor (#38813).
