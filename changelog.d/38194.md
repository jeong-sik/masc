### Fixed

- A Codex usage-limit refusal now dates its quota window from the reset the
  app-server's `account/rateLimits/updated` notifications named, so a demoted
  Codex candidate is tried again once its window opens (#38194).
