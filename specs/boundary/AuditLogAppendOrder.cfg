\* Audit log append boundary -- clean model.
\* Mutex-guarded append: at most one fiber inside the critical section.

SPECIFICATION Spec

CONSTANTS
    Fibers = {1, 2}

INVARIANTS
    TypeOK
    MutexExclusion

CHECK_DEADLOCK
    FALSE
