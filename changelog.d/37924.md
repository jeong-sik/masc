### Fixed

- A config that names the native `claude` provider now reads the catalogued Claude model rows, so it gets the model's 1M context window and 128k output ceiling instead of the Anthropic preset's 200k/8192. Only the catalog is read on this path, and other providers are unchanged (#37924).
