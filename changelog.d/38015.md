### Fixed

- Task Review now says which verdict each request is waiting for, and a cancellation carries the case for stopping into the detail pane under `WHY IT SHOULD STOP`. The VERDICT column read a field the verification queue never sends, so it was blank on every row ever drawn and the detail pane said `not joined (history view)`: a request to stop a task looked exactly like a completion claim short of evidence (#38015).
