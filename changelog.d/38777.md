### Added

- RFC-event-queue-admit-all-ready §7: a turn's stimulus admission is bounded by a declared `admission_budget_bytes` (runtime TOML), enforced in the admission loop before a source enters the turn — never by trimming rendered context. The count cap it replaces was the pinned observation section's only byte bound (#38176).
