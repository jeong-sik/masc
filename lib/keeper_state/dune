; RFC-0056 Phase 1N — extract Keeper_state_* deterministic FSM cluster from
; lib/keeper/. Fourth lib/keeper/ promotion after keeper_event_queue,
; keeper_invariant, and compaction_trigger.
;
; This library owns the pure keeper lifecycle FSM, JSON wire encoders, Mermaid
; rendering, and prompt state-block helper. Deps stay leaf-level: Yojson for
; wire/evidence values, masc.attribution for transition attribution, and
; masc_log for the existing compaction no-savings warning.
;
; (include_subdirs no) opts out of parent's (include_subdirs unqualified).
; (wrapped false) keeps the bare `Keeper_state_*` module identifiers.
(include_subdirs no)

(library
 (name masc_keeper_state)
 (public_name masc.keeper_state)
 (wrapped false)
 (libraries
  masc.attribution
  masc_log
  yojson))
