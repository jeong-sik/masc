### Fixed

- Tool input validation checks nested objects and array items before
  dispatch and reports every violation with its JSON path, instead of leaving
  the handler to report one problem per round trip (#38391).
