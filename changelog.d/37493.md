### Fixed

- `keeper_skill_validate` no longer fails every call with "tool output artifact
  storage failed". Its result now names the validated source by digest instead
  of echoing a normalized artifact reference, so the verdict stays inline for
  the model (#37493).
