{
  "id": "l1-node-sum",
  "level": "L1",
  "lang": "node",
  "timeout_sec": 900,
  "verify": "verify.sh",
  "prompt": "The check in this workspace fails. Run `bash check.sh` inside the workspace directory to see the failure, then fix sum.js so the check passes. Do not modify check.sh or check.js. Reply DONE once `bash check.sh` prints PASS.",
  "description": "Single-file bug fix: sumUpTo multiplies instead of adding; fix sum.js so check.sh passes."
}
