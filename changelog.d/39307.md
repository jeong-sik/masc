### Fixed

- A rejected or unreadable Skill configuration, which leaves every Keeper with
  no Skills, now degrades `/health?full=1` through a `skill_catalog` section.
  Its operator action reasons carry each diagnostic and the runtime.toml path
  in the same words as the boot WARN and the save-path 400 from #39274. A
  Skill refresh, Skill editor save or Keeper Skill publication that rereads a
  hand-edited runtime.toml after boot now logs the config state change once
  (#39307).
