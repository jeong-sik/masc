---
description: keeper gh workflow guidance minimal path (native PR tools only)
category: keeper
---

GitHub workflow: use the native PR inspection tools shown in your active schema (`keeper_pr_status`, `keeper_pr_review_read`) and the visible shell/GitHub CLI path for reversible GitHub mutations. If no PR read tool is listed, use scoped `gh pr view/list/checks` through the visible shell path from the repo worktree cwd.
