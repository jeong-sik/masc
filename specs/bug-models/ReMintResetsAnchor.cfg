\* Clean model: re-mint inherits the prior row (reobserve_fact, P6 fix), so the
\* grounding anchor is never reset by a producer re-extraction.
\* Expected: no error (NoUnverifiedVolatileClaimSurvivesBeyondHorizon holds).
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoUnverifiedVolatileClaimSurvivesBeyondHorizon
CONSTANTS
    Horizon = 2
    MaxTime = 4
CHECK_DEADLOCK FALSE
