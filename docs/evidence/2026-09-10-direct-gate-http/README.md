# Real Gate continuation through a CI binary

CI run 34412127768 supplied macOS artifact 10127941785 for exact binary commit `e95ce5b1e15b19c328db704af72d775ff574bc2d`. The harness checked its manifest, executable SHA-256 and running `/health` identity. It submitted one synthetic request through public MCP, created a real Write effect followed by an Execute Gate request, then returned a provider HTTP 429. The original operation remained queued with its exact checkpoint, approval identity and frozen alternate runtime.

An authenticated public Gate approval resumed that same operation on the alternate runtime. The prior Write was not repeated. The approved Execute appended its fixture bytes once. Its durable output reference contained the full 64-character SHA-256 for 655 bytes. The resumed provider input contained that exact reference; a real `keeper_artifact_read` call returned the complete matching bytes with EOF. The original operation succeeded with its unchanged input digest and one persisted user/final assistant row.

The original harness receipt says `verified` for those operation checks. Post-shutdown inspection additionally found a fifth provider request: a reactive turn for the same leftover approval stimulus. It reused the recorded replay outcome without executing the effect again. Therefore this evidence proves the bounded same-operation/replay/artifact path, **not absence of a redundant follow-up turn**. `runtime-observation.json`, the selected log and post-shutdown transcript retain that finding. The subsequent source repair records the existing continuation settlement after a successful direct turn; its CI and new-runtime verification are separate pending work.

Earlier probes remain recorded: run 1 failed because Colima could not bind the `/private/tmp` identity file; runs 2 and 3 completed the operation but exposed harness assumptions about refreshed system observations and approval-lifecycle rows. The harness now accepts a caller-selected fresh shared runtime base, distinguishes admitted user input from refreshed context, and distinguishes approval lifecycle provenance from operation provenance. No existing operation was resubmitted.

This uses a synthetic provider and real server, queue, sandbox effect, checkpoint, resolver and artifact reader. It does not prove semantic LLM judgment, attachment or channel-session behavior, restart, automatic recovery from unavailable authority, compaction, or long-duration continuity. Raw isolated recordings remain under `/tmp/masc-direct-gate-http-e95-4`; only synthetic inputs and selected evidence are included here. No local build was performed.
