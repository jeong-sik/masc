### Fixed

- A Codex `thread/tokenUsage/updated` breakdown whose counts do not nest (more cached input than input, more reasoning than output, or output or cache writes without input) now fails the turn as a protocol error of that frame, like a missing count. Before, such a breakdown was read as a request of zero input and its counts reached the usage ledger (#39062).
