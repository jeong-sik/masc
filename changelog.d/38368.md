### Fixed

- A successful Keeper turn logs its keeper in the structured `keeper_name` field and names the runtime that answered; it logged `system` and the redacted `runtime_lane=runtime` before (#38368).
