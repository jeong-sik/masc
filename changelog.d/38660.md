### Added

- The GitHub login tab offers three more scopes a login may ask for — `read:packages`, `project`, and `write:repo_hook` — each with its own digit toggle and a note saying what the token could do with it (#38660).
