\* Cascade strategy boundary contract — clean model.
\* Verifies: TypeOK, BoundedCycle, TerminationConsistency.

SPECIFICATION Spec

CONSTANTS
    Candidates = {"a", "b", "c"}
    MaxCycles = 3

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
