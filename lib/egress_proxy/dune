(include_subdirs no)

; The CONNECT decision, kept apart from any socket. A security check that can
; only be exercised by opening a listener does not get exercised.
(library
 (name masc_egress_proxy_decision)
 (public_name masc.egress_proxy_decision)
 (wrapped false)
 (flags
  (:standard -w +4 -w +33))
 (libraries masc.egress_host)
 (modules egress_proxy_decision))
