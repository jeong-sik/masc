{
  "id": "l1-bash-classify",
  "level": "L1",
  "lang": "bash",
  "timeout_sec": 900,
  "verify": "verify.sh",
  "prompt": "The check in this workspace fails. Run `bash check.sh` inside the workspace directory to see the failure, then fix classify.sh so the check passes. Do not modify check.sh. Reply DONE once `bash check.sh` prints PASS.",
  "description": "Single-file bug fix: classify.sh calls numbers over 10 small; fix the comparison so check.sh passes."
}
