\* Buggy model: CrashToDead skips the drain cascade; SloppyFinishDrain
\* ignores the held-task check.
\* Expected: NoDeadKeeperHoldsTask (and ClaimerNotDead) VIOLATED.
SPECIFICATION SpecBuggy
CONSTANTS
  K = {k1, k2}
  T = {t1, t2}
INVARIANT TypeOK
INVARIANT NoDeadKeeperHoldsTask
INVARIANT ClaimerNotDead
CHECK_DEADLOCK FALSE
