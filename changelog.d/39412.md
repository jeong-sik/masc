### Performance

- Count claimed and in-progress tasks once per execution render for the supplied worker names, preserving exact ownership and attention-row behavior. End-to-end latency remains unmeasured. #39412
