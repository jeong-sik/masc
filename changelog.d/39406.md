### Fixed

- Ordinary uninterrupted TUI Enter sends now retain separate request identities and durable FIFO queue order, including sends made before a control token arrives. A stop or resume may retain earlier local messages for an operator decision; explicit `/run-next` remains available for priority (#39406).
