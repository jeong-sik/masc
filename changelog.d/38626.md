### Fixed

- The `skill_delete` audit row records what happened to the package folder (`package_directory`), so a later `package_already_exists` for the same id can be traced after the WARN log rotates (#38626).
