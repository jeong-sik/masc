[keeper]
autoboot_enabled = true
# sandbox_profile = "local" (not "docker") for the one-touch preset — see
# presets/README.md § classic for the Docker-in-Docker rationale and the
# host-native override.
sandbox_profile = "local"
network_mode = "inherit"
