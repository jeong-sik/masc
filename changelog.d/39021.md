### Documentation

- Glossary: adds Activation Mode, the closed three-value owner policy
  (`manual` | `on_demand` | `autonomous`) the TUI settings editor now
  validates before sending, and Prompt Block, the structured input
  (`Prompt_block_id`) whose dashboard decoder list must name every id a
  Keeper turn carries (#39021).
