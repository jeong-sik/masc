[keeper]
base = "base.toml"
# autoboot_enabled = true because base.toml is now autoboot_enabled=false
# (loader template, not bootable).
autoboot_enabled = true
persona_name = "issue_king"
sandbox_profile = "local"
network_mode = "inherit"
