\* The recovery the code runs, except that it raises the position's counted
\* lines to the log's length.
\* NoAtomPassedUnread MUST be violated: raising the count swallows a restart
\* line no round has taken in. The code leaves the count alone
\* (Keeper_checkpoint_purge.librarian_rebase).
SPECIFICATION SpecPurgeTrimAtEndCountingLines
CONSTANTS
    MaxTurns = 3
    MaxSaves = 2
    MaxClears = 1
    MaxBad = 0
INVARIANT TypeOK
INVARIANT NoAtomPassedUnread
CHECK_DEADLOCK FALSE
