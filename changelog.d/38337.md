### Added

- `masc keeper-github login` takes `--scope`, repeatable, with the scope names the TUI GitHub tab and the dashboard panel offer (`workflow`, `write:packages`). The CLI login used to ask only for gh's minimum, so logging a Keeper in again from the shell replaced a token that carried `workflow` with one that did not. A name the server does not offer is refused before the device flow starts (#38337).
