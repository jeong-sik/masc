### Fixed

- The server no longer asks GitHub for each open pull request's title and
  head branch, no longer requires them when decoding a pull request, and no
  longer sends them on `GET /api/v1/repositories/pulls`; the TUI no longer
  decodes them. No screen drew either field, so a pull request without a
  title now counts as open instead of undecodable (#38507).
