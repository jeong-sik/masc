### Internal

- `Keeper_sandbox_image_catalog` parses a per-host `sandbox-images.toml`
  that maps a sandbox image name to the build promoted for each image store,
  and resolves a name to that build or to a typed reason it is missing.
  Nothing reads it yet (#38745).
