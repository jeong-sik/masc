### Fixed

- A mouse wheel notch over an open TUI reading (a memory fact, an event's
  evidence, a diff, a detail beside its list, a patch or link modal) scrolls
  that reading one row, as `j` does, instead of moving the list behind it.
  The loop keeps the reader the presented frame reported through
  `clamped_scroll`; rows of a list drawn beside a reader keep the list's
  wheel (#39240).
