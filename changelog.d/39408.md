### Fixed

- Apply Antigravity re-login changes at the same OAuth source path through private account generations, preserving native token refresh and existing in-flight clients. Keeper binds sessions to the selected generation and publishes native permissions only after winning its turn claim.
