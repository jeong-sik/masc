### Changed

- The Antigravity stderr tail that rides into error details is redacted line
  by line: a line carrying a credential marker (an Authorization header, a
  bearer token, an API key, a home path) is replaced wholesale with
  `[redacted]` before it can reach the session log or the dashboard. All
  other lines are kept byte-identical (#39245).
