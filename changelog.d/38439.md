### Added

- The TUI watches the server's DOS machine: `%` (or `:go dos`) opens a
  read-only spectator showing the program, the controller holder and the
  step count; watching never moves the machine (#38439).
