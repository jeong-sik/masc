### Removed

- The Keeper runtime contract JSON, the briefing keeper summary and the operator persistent-agents snapshot no longer carry a `generation` key; Keeper meta has none, so it was always null (#38460).
