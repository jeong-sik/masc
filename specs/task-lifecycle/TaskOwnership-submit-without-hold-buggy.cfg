\* Carried over from TaskLifecycle.tla (BugSkipClaim): a submission from an
\* agent that never held the Task must be caught.

SPECIFICATION SpecBugSubmitWithoutHold

CONSTANTS
    Tasks = {t1, t2}
    Agents = {a1, a2}
    MaxSubmissions = 2
    NoOne = NoOne
    Operator = Operator

INVARIANTS
    SubmissionRequiresHold

CHECK_DEADLOCK
    FALSE
