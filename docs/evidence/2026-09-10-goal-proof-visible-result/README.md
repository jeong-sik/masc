# Visible Goal proof result

The actual exhibition Goal returned from Verifying to Executing because its
verifier could not visually inspect the files. The old Goal detail showed the
phase and a completed linked Task, but did not display that proof result.

The detail now renders the existing criterion-bound proof status and the full
current refutation reason above its lifecycle controls and tabs. Historical or
unreadable proofs keep their distinct existing status; no confirmation authority
or Goal criterion changes.

Validated by TypeScript no-emit check and actual Playwright browser against the
unchanged isolated backend9cc33feff4. The displayed reason matches the durable
Goal proof record exactly. before.png/after.png are source-preview screenshots,
not installed frontend release evidence. receipt.json records source file hashes.
