### Fixed

- The terminal-field guard can see past a call that sanitises one argument. Naming a call in its allowed list exempted the whole argument list, which is right for a call that escapes everything it is given and wrong for `box_wrapped_field buf cols ~head ~style body`, which reads `body` line by line through `wrap_body ~sanitize` and concatenates `head` raw -- so the ask pane, the surface that draws what a Keeper wrote, could not be added to the list at all. `count_field_accesses_outside_sanitised_calls_in_value_binding` takes the arguments a listed call does not escape and walks those at the depth the call was reached at; the plain form is that one with an empty list, so no existing caller changes. The ask pane's fields are guarded now, two of them as the names they are matched out of, and dropping `single_line` from the head that draws `ar_keeper` fails the guard where it used to pass (#38343, closes #38275).
