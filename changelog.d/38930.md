### Fixed

- A Codex turn that fails after the app-server has reported usage now leaves one raw cost-ledger row per model response, and a successful multi-response turn no longer keeps only its last response. The Keeper writes each `thread/tokenUsage/updated` frame when it is read, and `Runtime_execution.usage_report` tells `AfterTurn` to skip the row the stream already wrote (#38930).
