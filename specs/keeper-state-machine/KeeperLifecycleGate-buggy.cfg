\* KeeperLifecycleGate model checking configuration (buggy).
\* Pre-fix short-circuit: the proactive gate is per-keeper only, so a
\* global "proactive off" with the per-keeper flag on still runs a
\* proactive turn — Safety is violated (RFC-0297 P0-1).

SPECIFICATION SpecBuggy

INVARIANTS
    TypeOK
    Safety
