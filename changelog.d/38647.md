### Internal

- Move stray evidence files to their conventional locations:
  `task560-evidence.md` into `evidence/task-560/`, the lone
  `scenarios/` JSON into its proof bundle, and `audits/task-1194-*`
  into `docs/audits/` (#38647).
