### Fixed

- A Librarian continuity snapshot file that cannot be decoded (a torn write,
  or a format another binary wrote) is now rebuilt from atom 0 and replaced
  on the next commit, instead of stopping every continuity pass until an
  operator deletes it. A snapshot file that cannot be read stays an error
  (#38477).
