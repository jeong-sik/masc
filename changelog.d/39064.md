### Fixed

- Enter on Tools reads the selected published Skill's retained evidence and the
  pane draws it under that Skill's row, and the key table named only the keys
  that write -- `c / C` for a new Skill, `e` to edit one -- so the key that
  reads was on no footer and in no cheat-sheet section. Its Enter-atom
  exception ("a scrolling reading, not a row list") stopped being true once
  `J/K` gave the screen a Skill cursor, and goes with it (#39064).
