### Fixed

- `/api/v1/dashboard/keeper-costs` counts the turn rows whose `ts_unix` or
  `latency_ms` it cannot read as `metrics_read.unplaced_turn_rows`, and lists
  Keepers whose meta it cannot read under `keepers_unread` with the reason.
  Both used to be dropped without a trace, leaving a floor that read as an
  exact sum (#38728).
