### Fixed

- The Board title says how big the board is, not how long one page is: it reads `(50 of 109)` where a page holds part of the board, and the plain count where the page holds all of it. The listing is one page with no key that reaches past it, so a title carrying only the page length left the posts beyond it with no trace on screen. Before the census answers, the page count stands alone (#37999).
