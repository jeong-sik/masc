### Fixed

- Execute reports a path refusal (`Path blocked`, the caller's cwd, cd or
  redirect target) as `policy_rejection`; it was `runtime_failure` through a
  local default. The keeper's remaining failure helpers and
  `Tool_args.error_result_typed` take a required class, and three unused
  `Tool_args` helpers that fixed a class are gone (#38705, refs #27742).
