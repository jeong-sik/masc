\* Carried over from TaskLifecycle.tla: Done without a verdict.

SPECIFICATION SpecBugDoneWithoutVerdict

CONSTANTS
    Tasks = {t1, t2}
    Agents = {a1, a2}
    MaxSubmissions = 2
    NoOne = NoOne
    Operator = Operator

INVARIANTS
    DoneRequiresLiveApproval

CHECK_DEADLOCK
    FALSE
