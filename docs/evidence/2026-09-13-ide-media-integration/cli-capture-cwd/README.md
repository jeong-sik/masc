The operator CLI compiled, but all three successful-media cases failed on Linux: absolute private capture directories were resolved under the restricted shell cwd capability. Initializing Process_eio with an fs-rooted current path preserves the shell default while allowing explicitly selected workspace paths, as existing verifier tests do. The remaining three negative CLI cases passed. Release macOS compiled but its verification-authority evidence test had no ffprobe/ffmpeg; Release now installs FFmpeg on both platform families before evidence execution. No native suite was skipped or weakened. Exact original failure identities and line-numbered excerpts are adjacent. Native rerun is required.
