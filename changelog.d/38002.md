### Fixed

- A repository that failed to sync now says what went wrong: the git message behind the failed clone or fetch is drawn in the selected row's context, under the Path and Keepers it already carries. The screen used to draw the bare word `error` in a nine-cell column, and the cause reached no screen at all. A status word this build does not know, and an `error` that names no cause, are shown as they arrived (#38002).
