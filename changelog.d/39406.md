### Fixed

- Ordinary TUI Enter sends now retain separate request identities and durable FIFO queue order, including messages staged behind a stop acknowledgement; explicit `/run-next` remains available for priority (#39406).
