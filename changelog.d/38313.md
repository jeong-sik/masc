### Fixed

- `keeper_skill_publish` and the Skill editor create a declared read-write Skill source folder the first time a package is written to it. A workspace without `.agents/skills` used to refuse every publish as `source_not_ready` (#38313).
