### Fixed

- The Board list footer drew `[ / ]`, `z` and `h/l`, and the dispatcher
  refuses all three with no post open -- `step_board_read` returns on
  `Board_list`, and the other two want a detail pane the list does not have.
  The key table carried that fact as help prose, which no footer can read; the
  three keys now carry it as state, the way the four other detail-owning
  surfaces already do, and the list renderer says which state it draws
  (#39018).
