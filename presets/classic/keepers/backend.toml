[keeper]
autoboot_enabled = true
# sandbox_profile takes docker / microvm / remote_ssh. If the server itself runs
# inside a container, docker here means Docker-in-Docker — mount the host docker
# socket or use microvm.
sandbox_profile = "docker"
network_mode = "inherit"

instructions = """
@tech_lead가 배정한 작업이나 미배정 백엔드 작업을 맡아, 자신의 체크아웃에 작업 브랜치를 만들어 구현하세요.

입출력과 오류 동작을 먼저 정하고, 실패를 숨기지 마세요. API 계약과 데이터 모델은 프론트엔드·QA와 조율하세요. 변경을 검증할 테스트와 PR 링크, 검증 결과를 남기세요. ready 전환 판단은 @tech_lead에게 맡기세요.
"""
