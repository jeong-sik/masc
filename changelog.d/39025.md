### Fresh state required

- Keeper metadata no longer carries `trace_history` or `last_handoff_ts`; both were always empty or zero. An existing Keeper meta file with either key is rejected as not current and needs a runtime reset. Skill activation owners report only `current_meta` or `runtime_manifest` (#39025).
