### Fixed

- Eight TUI places that drew `?` for a missing value (a backlog's oldest
  age, a gate row's waiting time, the Keeper calls title with no Keeper, the
  voice wizard's step and probe fields, the scheduler pool size) now draw
  the shared no-value mark `—`. A voice probe state the build does not know
  is shown as written (#39258).
