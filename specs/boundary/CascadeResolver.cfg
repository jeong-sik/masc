\* Cascade resolver clean model.
\* Verifies health-aware routing and capability filtering.

SPECIFICATION Spec

CONSTANTS
    Providers = {p_fast, p_slow}
    Capabilities = {"chat", "tool"}
    ChatProviders = {p_fast, p_slow}
    ToolProviders = {p_slow}
    InitialUpProviders = {p_fast}
    NoProvider = no_provider

INVARIANTS
    SafetyInvariant

CHECK_DEADLOCK
    FALSE
