# MASC-MCP Code Audit — 2026-02-03

## Session Summary
- Conversation Layer: Phase 1 complete (types, file CRUD, loop guard, dual-stream Neo4j, MCP tools)
- Fixed critical path bug: `conversations_dir` used `config.base_path` instead of `Sys.getcwd() ^ "/.masc"`
- Fixed `thread_persist.ml`: rewrote Neo4j HTTP client (cypher injection, pipe-safe reads, JSON parsing)
- Added 15 new tests (8 Conversation + 5 LoopGuard + 2 ThreadPersist) → 46 total council tests

## Code Audit Results (125 ML/MLI files, ~48,575 LOC)

### CRITICAL

| # | Category | File:Line | Issue |
|---|----------|-----------|-------|
| 1 | Shell Injection | `council/executor.ml:127` | `Sys.command (sprintf "mkdir -p %s" dir)` — Use `Unix.mkdir` |
| 2 | Shell Injection | `room_git.ml:118-132` | Branch name injected into shell — Use `Eio.Process.spawn` |
| 3 | Shell Injection | `auto_responder.ml:126-129` | Incomplete JSON escaping in shell — Use temp file |
| 4 | Obj.magic | `test_tool_relay_coverage.ml:27` | `sw = Obj.magic ()` — Use real `Eio.Switch.run` |
| 5 | List.hd crash | `lodge_heartbeat.ml:460,1798,1963` | Unchecked `List.hd errors` — Use pattern match |
| 6 | List.hd crash | `spawn.ml:209` | `List.hd cmd_args` could be empty |
| 7 | List.hd crash | `tool_lodge.ml:280,1184,1568` | Multiple unchecked `List.hd` on JSON data |

### HIGH

| # | Category | File:Line | Issue |
|---|----------|-----------|-------|
| 8 | Bad Async | `tool_lodge.ml:1274,2008` | `Unix.sleepf` in Eio context — Use `Eio.Time.sleep` |
| 9 | Bad Async | `bin/main_eio.ml:1839,1848,1873` | Direct `Unix.sleepf` blocks event loop |
| 10 | Bad Async | `backend_eio.ml:578; bounded.ml:325,334; room_utils.ml:785,818` | `run_in_systhread` for sleep |
| 11 | Resource Leak | `room.ml:153-156; auto_responder.ml:103-106` | Process handle not closed on exception |
| 12 | Resource Leak | `audit_log.ml:113; cache_eio.ml:100-104; encryption.ml; federation.ml; voice_bridge_eio.ml` | `open_out` without `Fun.protect` |
| 13 | Memory Leak | `cancellation.ml:100-102` | Hashtbl tokens never cleaned up automatically |
| 14 | Global State | `mcp_server_eio.ml:29-60` | `current_net` crashes if `set_net` not called |
| 15 | Missing Timeout | `auto_responder.ml:114-132; neo4j_client_eio.ml` | No timeout on MODEL/GraphQL calls |

### MEDIUM

| # | Category | Issue |
|---|----------|-------|
| 16 | Bare exception | 15+ locations with `with _ ->` swallowing errors |
| 17 | Hardcoded vals | Thresholds in `tool_mitosis.ml`, heartbeat timeouts |
| 18 | Long functions | `room.ml:join` ~150 lines, `lodge_heartbeat.ml` 100+ line blocks |
| 19 | Code duplication | File lock pattern repeated in `room.ml` claim/complete/release |
| 20 | Fun.protect misuse | Nested `Fun.protect` where `Switch.on_release` is better |

## Top 5 Fix Priorities

1. **Shell Injections** (3 files) → `Eio.Process.spawn` with arg array
2. **List.hd crashes** (7 instances) → pattern matching
3. **Resource leaks** (8+ files) → `Fun.protect` / `Switch.on_release`
4. **Remove Obj.magic** (test) → proper test fixture
5. **Add timeouts** (external calls) → `Eio.Time.with_timeout`
