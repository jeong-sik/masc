\* Buggy spec — pre-Phase-B PR-4 production reality.  ViolatedSilentRepeat
\* is reachable: NoSilentContractRepeat violated by
\*   Init -> EnterTurn(k1) -> ResponseReceived(k1) -> ViolatedSilentRepeat(k1)
\* (depth 4).  Concurrently ViolatedImpliesFeedback is also violated
\* because the trace leaves contractViolated[k1]=TRUE while
\* feedbackLogged[k1]=FALSE.  TLC must report a counterexample —
\* the witness that Phase B PR-4's chat-store feedback emission is the
\* correct fix.

SPECIFICATION SpecBuggy

CONSTANTS
    Keepers = {k1, k2}

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
