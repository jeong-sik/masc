### Changed

- A queued person's chat waits for an in-flight Keeper provider turn to reach a settled tool boundary, finish, or hit its configured timeout, even when that turn has not produced its first response event (#39309).
