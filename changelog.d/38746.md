### Removed

- `/burn` and its tab-row cost and token bars are gone. Its `$` added up each
  Keeper's lifetime cost, which took a turn with no price as $0, and the
  fleet's 24h cost is on the Overview Team line through `/team-cost`, which
  `/cost` now opens (#38746).
