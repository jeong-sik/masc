### Internal

- The pull-request path of the edited-test runner now drops the hook that only `--self-test` sets, so an inherited environment variable cannot hand a second suite the long timeout reserved for the keyboard walk (#38591).
