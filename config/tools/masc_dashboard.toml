name = "masc_dashboard"
description = """
Return a concise workspace dashboard summary for the current project. Use \
scope to choose the current task-focused view or the full workspace view."""
additional_properties = false

[[params]]
name = "scope"
type = "string"
enum = ["all", "current"]
description = "Dashboard scope: current or all"
default = "current"
