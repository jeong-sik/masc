\* Keeper counter causality — clean model
\* Clean: Safety (TypeOK + CausePresentWhenCounted) must hold.

SPECIFICATION Spec

CONSTANTS
    MaxCount = 4

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
