Exact media source7bd85f6bd6 Test34707024488 ran all ten requested suites: 182 of 184 cases passed. PPTX failed before parsing/rendering because RUNNER_TEMP was under HOME; tool-plan expected names omitted the existing BrowserGoto JSON contract. Commit04bdb26e8f selects the system temporary directory in the fixture producer and consumer and adds the missing expected name. It leaves the HOME write guard and product tool descriptor intact. Native rerun remains required. The excerpt preserves selected message lines, with trailing whitespace normalized; diagnosis.json records their original positions and the full original log SHA.

The first path correction was insufficient for Dune aliases: Dune overrides TMPDIR before spawning children. The final fixture producer records its canonical base in RUNNER_TEMP/masc-presentation-verifier.json only after successful setup. Both native and Python consumers read that descriptor, so alias-specific temporary directories cannot redirect them. The descriptor is read under RUNNER_TEMP; workspace writes remain outside HOME. Five Python tests cover a changed child TMPDIR, invalid descriptors and failed publication preserving the prior descriptor. This is not a local native/Dune run. Dune source evidence is in dune-tmpdir-review.json.
