\* Closing a Task while leaving its holder in place must be caught.

SPECIFICATION SpecBugCancelKeepsHolder

CONSTANTS
    Tasks = {t1, t2}
    Agents = {a1, a2}
    MaxSubmissions = 2
    NoOne = NoOne
    Operator = Operator

INVARIANTS
    ClosedOwesNothing

CHECK_DEADLOCK
    FALSE
