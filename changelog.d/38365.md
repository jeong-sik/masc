### Fixed

- A Keeper logs "turn start unknown" only when an unknown turn start actually opened the request range; reading the turn start on every dispatch and in the next-request forecast no longer raises it (#38365).
