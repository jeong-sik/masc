SPECIFICATION Spec

CONSTANTS
    Threshold = 3
    ErrorClasses = {"path_not_found", "path_not_allowed", "other"}
    MaxSteps = 12

INVARIANT SafetyInvariant

\* Action property: a trip fires only on a genuine same-class run of length
\* Threshold. The clean spec reaches trip states, so this is exercised, not
\* vacuous.
PROPERTY TripOnlyWithStreak

CHECK_DEADLOCK FALSE
