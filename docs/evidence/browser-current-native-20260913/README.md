# All-native 3446 observation cohort

Source `3446d8a35ee132eebadcadc63fc77d36e4eafead` supplied the native
server/TUI/host/extension and binary-exported browser Skills. Full component hashes
and exports are preserved in bundle.json/report.json. Startup used the original
/var alias successfully, without the previous ROOT.resolve() harness workaround.
This is an isolated experiment, not deployment or a headed operator session.

Six outer calls, zero errors, three compositions, 31,845 raw result bytes and four
exact retained scenes were observed. TUI replay recorded 116 complete frames and
all three channels without input after Keeper start. Elapsed time was 105.554
seconds: this single run was slower than preceding cohorts and is not causal
performance evidence. Timing includes model and observation overhead.

The terminal answer is preserved unchanged. Beta's current decision is a plan,
not proof migration completed. Inference/order is labeled, but does not establish
a cross-channel dependency. Semantic correctness is manual review, not a universal
algorithmic result. Slack, BiDi/native pointer, and deployment are not covered.

Run `python3 audit.py` from any directory after archive extraction. It joins
actual execution IDs/raw replies, checks byte-exact delivered JSON against four
retained blobs, and verifies component identities, TUI prefix/lifetime and checksums.
Textual TUI screenshots are native PTY replays; firefox-final.png is a separate
actual Firefox capture, not an atomically aligned image. No profile, credentials,
provider configuration or complete provider trace is retained.

Temporary probe and capture-helper files were copied after the run; no pre-run
wrapper hash was recorded.
Candidate-source-proof covers committed sources, not those temporary wrappers.
Arguments used source3446, server/TUI under /tmp/browser-live-context-native-3446/runtime,
checkout /private/tmp/masc-browser-live-context-native and bundled-skills under
/tmp/browser-live-context-native-3446/bundled-skills. These historical paths are
not required by the offline audit and are not portable runtime instructions.
