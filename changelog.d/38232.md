### Fixed

- The retained checkpoint sweep no longer skips itself on every startup. It
  read every directory under `keepers/` as a keeper, and the workspace store
  `keepers/tool_usage/` holds an empty `chat-operations.sqlite3` that the
  operation store refuses. Workspace-scoped store directories are now skipped;
  a keeper named like one of them stops the pass with nothing removed
  (#38232).
