### Changed

- The chat heading and the runtime picker header no longer say `FAILOVER`, and the `media_failover` line no longer calls the vision fleet's call order by the runtime order's name (#37918).
