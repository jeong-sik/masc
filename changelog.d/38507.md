### Fixed

- The Overview pull request decoder no longer requires each PR's title and
  head branch, which no screen draws, so a PR the server sends without a
  title counts as open instead of unreadable (#38507).
- `scripts/tui-decoded-field-census.py` lists record fields a TUI decoder
  fills and nothing reads, over `lib/tui_decode.ml` and the `bin/` decoders
  that call the JSON field helpers themselves (#38507).
