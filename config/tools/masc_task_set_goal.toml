name = "masc_task_set_goal"
description = """
Assign an existing, currently goalless task to a goal. Both task_id and \
goal_id are required and validated against the backlog and the goal store; \
an unknown id is rejected (never silently ignored or auto-picked). A task \
that already has a goal is rejected — reassignment is out of scope."""


# Held back from the request until the model asks for it by name.
# 2026-09-01..03: 3 turns needed it across 6317 provider requests, and its
# 567 B schema rode every one of those requests. Loading it on demand costs
# one extra request on those 3 turns and saves the 567 B on the rest.
defer_loading = true
[[params]]
name = "task_id"
type = "string"
required = true
description = "ID of the task to assign"

[[params]]
name = "goal_id"
type = "string"
required = true
description = "ID of the goal to assign the task to"
