(* Stamped by scripts/build-shim-static.sh: that script overwrites this
   file in its scratch project with ["+<short sha>"] before building, so the
   static artifact names the commit it came from. In-repo builds keep the
   empty suffix; they never run inside a guest. *)
let suffix = ""
