### Fixed

- `dune build @check` compiles `test_exact_output_flow.ml` again: the
  total-deadline case passes its fixture body timeout as the `float option`
  that `catalog_entry` takes since #38573 (#38757).
