\* Buggy spec — pre-Phase-B PR-4 production reality.  EmptySilentSkip
\* is reachable: NoEmptySurfaceWithoutFeedback violated by the
\* trace Init -> EnterTurn(k1) -> EmptySilentSkip(k1).  Concurrently
\* EmptySurfaceImpliesFeedback is also violated because the same trace
\* leaves surfaceEmpty[k1]=TRUE while feedbackLogged[k1]=FALSE.
\* TLC must report a counterexample — this is the witness that Phase B
\* PR-4's chat-store feedback emission is the correct fix.

SPECIFICATION SpecBuggy

CONSTANTS
    Keepers = {k1, k2}

INVARIANTS
    Safety

CHECK_DEADLOCK
    FALSE
