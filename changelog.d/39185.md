### Fixed

- A floor seed — the boundary fact a turn left at the zero-prior-history floor — now names no front in the type instead of carrying an empty-string digest: `Keeper_carried_front.seed.front_digest` is an option, while `for_history` keeps reading a turn record's null `front_atom_digest` as an absent one (#39185). Stored seeds round-trip: the JSON writes `null` for the absent digest and still reads the #39166 shape.
