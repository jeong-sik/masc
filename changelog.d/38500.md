### Fixed

- The world-state briefing budget is a share of the smallest
  `max-prompt-bytes` any candidate of the keeper's lane declares, not of the
  lane head's alone, so a fallback with a smaller ceiling that serves the
  turn while the head is demoted no longer receives a briefing sized for the
  head. A candidate that declares no ceiling adds no bound and does not erase
  a sibling's (#38500).
