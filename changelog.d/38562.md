### Fixed

- A failed keeper cycle no longer names a runtime as the one that ran when the agent_core pipeline refused to send its first request (`Attempt_rejected`, e.g. a missing output ceiling) and no request of that attempt went out; an attempt refused after an earlier request reached the provider is still attributed to that runtime (#38562).
