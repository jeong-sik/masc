### Internal

- Deleted `Constants.Http.cascadable_codes` from agent_core. Nothing read it; the lane walk decides from `Retry`'s typed classification, and the only reference was a test restating the literal (#38678).
