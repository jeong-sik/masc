### Fixed

- `keeper_surface_read` no longer refuses a gate lane whose rows are all
  older than the newest page. While older rows remain it returns an empty
  page with `has_more` and `oldest_ts`, so the keeper can page back to the
  lane; a label is refused only when the page holds the whole history
  (#38218).
