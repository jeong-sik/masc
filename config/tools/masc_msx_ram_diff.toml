name = "masc_msx_ram_diff"
description = """
Compare memory with the last masc_msx_peek snapshot.

Returns address, length, before/after hex runs and total changed bytes. At most 64 runs; truncated marks overflow. Machine swaps retain the snapshot."""
additional_properties = false

defer_loading = true
