\* KeeperLifecycleGate model checking configuration (clean).
\* RFC-0297 P0-1: the proactive gate is global AND per-keeper, so a
\* proactive turn implies both switches on and Safety holds.

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Safety
