\* Buggy model: resolver ignores provider health and can route to DOWN.
\* Expected: NeverRouteToDown MUST be violated.

SPECIFICATION SpecBuggy

CONSTANTS
    Providers = {p_fast, p_slow}
    Capabilities = {"chat"}
    ChatProviders = {p_fast, p_slow}
    ToolProviders = {}
    InitialUpProviders = {p_fast}
    NoProvider = no_provider

INVARIANTS
    NeverRouteToDown

CHECK_DEADLOCK
    FALSE
