\* Buggy: NoOrphan MUST be violated. If it passes, invariant is too weak.
\* Terminating spec; suppress the intended terminal-state deadlock so the
\* only failure signal left is the safety violation.
SPECIFICATION SpecBuggy
CONSTANTS
    MaxLen = 5
    MaxCount = 3
INVARIANT NoOrphan
CHECK_DEADLOCK FALSE
