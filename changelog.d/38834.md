### Fixed

- Existing v1 Keeper metadata without the newer usage cursor and last usage
  resolution fields now loads without resetting counters, pause state, or task
  binding. A schema-invalid JSON metadata file still reports the Keeper,
  path, and reason before fail-open treats it as absent (#38834).
