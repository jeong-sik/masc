### Fixed

- A turn whose cause is known no longer reports the operator disposition
  `unknown`: effect fences, lost tool corrections and failed terminal effects
  report the new `effect_review_required`, transcript corruption reports
  `operator_action_required`, and the runtime trust snapshot's fallback reports
  `operator_action_required` instead of `unknown` (#38442).
