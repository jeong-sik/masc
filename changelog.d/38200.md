### Fixed

- A standalone lane's detail says how many of its finished runs the slot selection history does not cover. The history counts only runs that named a slot, and the run total sits one line above it: on the live Board Attention lane 489 runs had finished and the slot counts added to 357, so a quarter of them were missing from one line with nothing saying why. Of that lane's newest 200 runs, 197 had finished and 47 named no slot -- 41 of them succeeded. The line now ends with `132 runs named no slot` when the counts fall short and says nothing when they do not, and it subtracts from the finished runs rather than the retained ones, so a lane mid-run does not report its running work as unnamed (#38200).
