### Fixed

- The prompt registry title keeps a held-back override count beside the
  prompt count instead of after it. The title row reserves a floor for the
  tab strip, so at 120 columns the reading could run one cell short and the
  fit-width cut the override count into an ellipsis -- an operator could lose
  an override without learning they lost it (#38713).
