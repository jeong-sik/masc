name = "masc_msx_screen"
description = """
Read the MSX frame without advancing time.

Returns mode, PC, text, a 64x24 ASCII view, tiles and sprites. Text is empty in bitmap modes; tile IDs depend on the game. Keeper calls include a PNG artifact and dimensions; use keeper_analyze_image for vision."""
additional_properties = false

defer_loading = true
