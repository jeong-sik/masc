### Fixed

- The TUI Schedules screen no longer draws a stale hold as current: while
  the schedule runner is not `ok`, a held schedule reads
  `held as of <time the runner last read it>`. Each row's `runner_hold`
  carries `observed_at`, the schedule list carries the runner's status as
  `schedule_runner`, and the TUI refuses a row without the `runner_hold`
  key (#38479).
