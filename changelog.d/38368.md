### Fixed

- A successful Keeper turn's log lines carry the keeper in the structured `keeper_name` field, and the cycle line names the runtime the run settled on instead of the redacted `runtime_lane=runtime` (#38368).
