### Fixed

- The Board title read `(50)` on a 109-post board: the number was the one server page a listing returns, and there is no key that reaches past it, so the page length stood where a reader looks for the board's size and the posts off the page left no trace. The title now states both where a page holds part of the board -- `(50 of 109)` -- and the narrowed hearth's size when one is being read; where the page carries everything it stays the bare count, and before the census answers there is no second number and the page count stands (#37999).
