### Fixed

- An Antigravity turn whose result event is an error now leaves a raw cost-ledger row for the usage that event reports; the report is emitted when the result is applied, before the turn is judged. With every official client reporting on its stream, `Runtime_execution.Client_turn_result` is removed (#38943).
