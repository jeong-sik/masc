### Fixed

- The Approvals surface title counted the approval rows alone while the tab badge counts approval rows plus the questions Keepers have open, so one open question and nothing held drew the tab as `Approvals·1` over a screen reading `MASC Approvals (0)`. The title now takes the same shared count and names the question beside the three approval kinds -- `(1 question)` (#38006).
