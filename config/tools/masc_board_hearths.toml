name = "masc_board_hearths"
description = "List active hearths (topic categories) with post counts"


# Held back from the request until the model asks for it by name.
# 2026-09-01..03: 9 turns needed it across 6317 provider requests, and its
# 150 B schema rode every one of those requests. Loading it on demand costs
# one extra request on those 9 turns and saves the 150 B on the rest.
defer_loading = true