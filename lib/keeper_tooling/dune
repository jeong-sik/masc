(include_subdirs no)

(library
 (name keeper_tooling)
 (public_name masc.keeper_tooling)
 (wrapped true)
 (modules
  execute_shell_ir
  name
  response
  shell_costume
  subset_rewrite)
 (flags
  (:standard -w +4 -w +33))
 (libraries
  masc.agent_core
  masc.bounded_event_dedupe
  masc.config
  masc.exec_policy
  masc.masc_exec
  masc.masc_exec_command_gate
  masc.masc_tool_dispatch
  ; [Subset_rewrite] advice templates are managed prompts
  ; (config/prompts/subset_rewrite.md); the masc.prompt_registry closure does
  ; not reach this library, so the edge is cycle-free.
  masc.prompt_registry
  masc.masc_log
  masc_core
  yojson))
