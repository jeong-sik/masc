### Fixed

- A rejected or unreadable Skill configuration, which leaves every Keeper with
  no Skills, now degrades `/health?full=1` through a `skill_catalog` section
  whose operator action reasons name each diagnostic and the runtime.toml
  editor that fixes it, and the log records the diagnostics once when the
  catalog enters that state and once when it recovers (#39307).
