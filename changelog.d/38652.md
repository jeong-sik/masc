### Fixed

- `lane-addon-native.yml` no longer triggers on pushes to
  `codex/lane-addon-v0`, a branch absent from the remote; the lane is
  explicitly manual-dispatch only, matching how it has actually run (#38652).
