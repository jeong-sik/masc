\* Audit log cache install boundary -- buggy model.
\* Mutex stripped so probe and install split: two fibers both see
\* empty cache and both install -> install_count >= 2.
\* Expected: CacheConsistency MUST be violated within <=4 steps.

SPECIFICATION SpecBuggy

CONSTANTS
    Fibers = {1, 2}

INVARIANTS
    CacheConsistency

CHECK_DEADLOCK
    FALSE
