{
  "id": "l3-py-rename-callers",
  "level": "L3",
  "lang": "python",
  "timeout_sec": 900,
  "verify": "verify.sh",
  "prompt": "check.sh fails: pricing.py was reworked to take a currency argument, but receipt.py still calls the old shape. Fix receipt.py (and anything else that still uses the old call) so the check passes. Do not modify pricing.py or check.sh. Reply DONE once `bash check.sh` prints PASS.",
  "description": "Multi-file fix: pricing.total(items, currency) changed shape; update the caller in receipt.py."
}
