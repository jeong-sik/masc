### Fixed

- A Keeper whose seeded range is refused with a 400 that agent core does not attribute (`Unknown_invalid_request`) is again asked once from the turn boundary. #38286 narrowed `refusal_evicts` to the typed size refusals, and the boundary resend shared that check, so providers whose size refusal arrives only as prose (ollama_cloud, glm) were refused on every turn. The resend now reads its own refusal set, `boundary_resend_on` (#38329).
