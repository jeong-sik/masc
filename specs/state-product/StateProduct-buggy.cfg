\* TLC Configuration — Buggy spec (BoundaryViolated MUST be violated)
\*
\* BugAction allows NonDet retry when turn is NOT Dispatching.
\* If BoundaryViolated is NOT violated, the invariant is too weak.

SPECIFICATION SpecBuggy

INVARIANTS
    TypeOK
    BoundaryViolated

CHECK_DEADLOCK
    FALSE
