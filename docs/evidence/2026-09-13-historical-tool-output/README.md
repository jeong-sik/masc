# Historical tool output and blob-backed Edit evidence

A retained autonomous turn can outlive the recent 200 tool-call responses. Its exact execution ID must fetch that stored execution rather than be treated as missing because it fell outside the window. The actual Edit0129 also stores its output as a 1345-byte artifact manifest, not an inline JSON string. Its artifact_refs collection lists after before before; the explicit manifest snapshots define before5237…12602B → after18e0…12641B. The manifest bytes and reference are preserved here.

The existing authenticated GET /api/v1/keepers/:name/tool-calls accepts a single execution_id query for exact lookup. It uses the derived index over the full retained ledger, with Keeper scope, returning 404 for absent, 409 for duplicate and 503 for storage failure. The ordinary recent route is unchanged. Actual HTTP subroute tests include 250 later calls, another Keeper with the same ID, duplicate rows, malformed query and a ledger path replaced by a file.

The Chat transcript receives its selected Keeper explicitly. Visible tool rows load missing output by Keeper and execution ID, reject mismatched responses, cancel stale requests on scope changes, retain local scoped success despite peer cache hydration, and allow explicit retry after missing/ambiguous/unavailable results. Externalized successful Edit results are fetched and SHA/byte verified before the typed manifest's structured result supplies its explicit before/after references. The existing original-file reader shares the same verified text-blob fetch. Raw manifest bytes are not replaced with a guessed ordering of artifact_refs.

Four affected UI suites passed 191 tests, the Dashboard TypeScript check passed, selected ESLint and changed OCaml parse-only checks passed. Independent review found a peer-cache refetch loop; a two-scope/third-hydration test now covers its repair. No P1/P2 findings remained in re-review. Log trailing blank lines are normalized; the artifact manifest is byte-exact.

Native HTTP tests, paired CI release artifacts and installed browser acceptance remain pending. This branch depends on autonomous execution projection f75aa4 (PR35547); that exact parent's native Test34701658049 passed. The current remote parent PR also contains a later main merge with an unrelated voice type failure, which is not this branch's base or verification evidence. No local production build or runtime upgrade was performed for this evidence.
