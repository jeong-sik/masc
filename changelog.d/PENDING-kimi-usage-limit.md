### Fixed

- Keeper lanes: an HTTP 403 now routes as `Authorization_refused`, apart from a 401's `Auth_failed`. The status alone still rests nothing. When the refusing provider declares `[providers.<id>.usage-read]`, the Keeper walk reads that endpoint once before moving on; a window whose used count reached its limit rests the account on its quota window until the window's stated reset, so later walks put it behind the candidates that can answer. A window with headroom, a failed read, or no `usage-read` leaves no evidence. Kimi For Coding answers its spent 5-hour window with a 403 (`{"error":{"message":…,"type":"access_terminated_error"}}`, no reset header), and its `/coding/v1/usages` states the window's `used`/`limit` and `resetTime`.
- `keeper cycle FAILED` logs `lane=` as the deferring assignment on a cycle that ran a deferred suffix, instead of the suffix's first runtime.
