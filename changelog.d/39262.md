### Fixed

- The TUI Keepers fleet header reads the fleet grade as a `Health_status`
  rather than comparing the string with `"ok"`, and the fleet scan builds
  the grade from the same type. A grade the build does not know is drawn as
  the server wrote it, on one line (#39262).
