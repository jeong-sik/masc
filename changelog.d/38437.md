### Fixed

- An exact-output walk (Librarian, HITL auto judge, Board attention, Verifier) now moves to the next slot when a sent request gets no answer within its slot deadline, instead of ending the pass at the first slot (#38437).
