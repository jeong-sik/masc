# Collaboration resumed on 2026-09-12

The old 84af integration candidate still had failed release expectations. Current repository state showed the associated image/peer changes and corrected expectations already merged, so it was superseded by the successfully built v0.35.12 release at703ce088c8548e5c2e74a4f682a5ccf6a9a586c4. Extra local assertions from the old candidate were preserved as a patch instead of republishing duplicate work.

The published macOS arm64 binary and runtime archive matched SHA256SUMS. The owned isolated server was gracefully replaced (PID30481 to7821), and health reported the expected source/hash, same base, and ready state. All17 captured Goal/Task/config/artifact file hashes matched immediately after replacement. This is a manual upgrade, not automatic failover.

The original Goal retry retained criterionc36f5a9061458b3efbeb8cbb2cacfc6b and was admitted ase106acb7d86b68c159ee18b0093d3ced. Actual evaluator run01a094e3-89fc-7000-9517-7a4e524dcc6e deferred afterKimi returned a weekly quota refusal, without tools or verdict. One historical malformed JSONL row remains; new run events were decoded independently.

Designer received the handoff requestkmsg-da6f4180e0eed96538d9ad77ee1bfb8d and actually called export. Both calls returned an artifact storage error. Independent inspection found the exact484694-byte poster blob already persisted: the failure is after storage, when typed result projection requires its missing durable result manifest. Recipient delivery remains unproven. This live failure triggered a source fix and a production ToolBridge-path test.

The original local27B process completed both Keeper groups and final synthesis (24sources,10claims,2conflicts). It correctly marks corrupted admission wording unresolved, but also introduces a three-files count from a source about three criteria. The source syllable example (U+BD05 in 바라봅니다) is valid and was checked independently. Its source revisions had changed during the run. This completed proposal is retained for review and has not been published as verified workspace knowledge.

## Product reassessment

[18-item reassessment and next steps](REASSESSMENT.md) separates historical evidence, current blockers, and the next acceptance sequence.

## Actual corrected export, followed by a new delegation failure

The CI-built83afd candidate from run34685872751 replaced the owned isolated server with PID8730. Health identified the exact binary hash and ready state. All17 observed state/config/artifact hashes matched immediately after upgrade. This is a manual candidate experiment; release_validated=false, and the corrected target suite did not execute because its pnpm setup step failed.

The designer's actual operationkmsg-e5f63dd058c494dcfa2991268295c0f9 reached Succeeded. Its export succeeded, then keeper_artifact_read read the durable1337-byte result manifest. Independent hashing verified that manifest and its484694-byte poster blob. The following delegate call supplied artifacts as strings and was rejected; the recipient file remains absent. Source investigation found two rich-schema projections that drop nested array/object details. That repair is separate from the now-exercised export manifest fix. See `peer-export-83afd.json` and `peer-export-manifest.json`. Operation success does not establish handoff success.

Separate product work: PR35372 implements configured official-client verifier routing and a real per-review system/tool context; dynamic image tool responses require a follow-up stack. PR35377 repairs the Tools crash on a nullable installed-binary repository path; TypeScript,23 focused tests, actual desktop search/clipboard and fresh mobile card visibility were verified. Its browser proof uses a Vite development preview, not a deployed or CI-built fix.

## Current 35be4 observations

The owned runtime is now candidate35be4 (v0.35.13). The configured Codex verifier actually read four unique PNGs and committed REJECT after70.006s: 3/5 criteria confirmed, PDF lookup failed as invalid UTF-8. Goal returned to executing/proof_refuted with the original criterion revision. See `goal-codex-35be4.json`; its failed lookup output hash is not a PDF source hash.

Designer export, typed delegation and editor materialization succeeded. Independent source/receiver hashes match for484694bytes. The editor posted durable Board commentc-6bf1718d8d35e2ad8c33e3b4a042c654 stating transfer success and image inspection failure. That helper has no configured media_failover candidates and currently excludes official clients. See `peer-delivery-35be4.json`. A successful chat operation is not a successful visual review.

Actual verifier TOML preview/save applied the manually selected Codex CLI route; this is not automatic failover. `runtime-config-35be4.json` preserves runtime identity and CI distinctions. The target13suites/433tests passed, but PR lint failed its deterministic-boundary ratchet at optional content_blocks projection. Neither merge readiness nor release validation is claimed.

The real local27B/24-source proposal was stored as model_proposed and read back unchanged after the owned runtime upgrade. Known semantic mistakes remain; no promotion or actual Keeper reuse is claimed. See `real-memory-draft-35be4.json`.

`goal-browser-35be4/` uses authenticated real backend responses and the downloaded CI preview from run34687175476. PR head35be4 matches the backend source; the preview checkout itself is GitHub merge commit9f72a01f, explicitly retained in provenance. Browser shows executing, refuted proof, the PDF error reason and verifier run identity. No synthetic Goal responses were supplied. No final-confirmation button was present. The preview was intercepted in the browser, not installed on the server: its artifact warning and the deliberately blocked WebSocket status remain visible. Screenshots were visually inspected; this does not prove general mobile usability.

## Actual shared memory use and next candidate

`memory-reuse-35be4/` records actual local27B proposal consumption by the editor, a saved handoff and memory/Board write, root-observed interpretation mistakes, subsequent correction, exact artifact delivery to the designer, and the designer's actual source-bound review and editorial plan on Board. Source-byte confirmation delegated back to the editor remains pending. This is operator-requested collaboration; spontaneous adoption and general semantic memory reliability are not established.

The next integration candidate is PR #35400 in `/tmp/masc-collaboration-media-20260912`. It combines PDF inspection (#35398), actual full-description API/UI (#35390), nullable Tools paths (#35377), and official-client vision (#35405). Previous candidate b38c executed 11 suites: the new PDF feature passed, while one obsolete PDF-text-error fixture failed; the test-only repair removes that outdated expectation and retains unsupported-binary journal replay coverage. Actual deployment/Goal/image-helper acceptance of the new combined candidate remains pending.

## Actual 53203 acceptance

The exact candidate's 15 targeted suites passed. Its CI macOS binary replaced the owned server; health matched source53203 and SHA494eb585..., and all22 captured files matched immediately after upgrade. The existing runtime.media_failover field was then manually set to Codex through a successful preview/save; parsed disk contents matched the proposal. No automatic failover is claimed.

The original publication Goal actually passed5/5. Codex read the real PDF and PNGs in run01a0953c-5e01-7000-9ed9-1003a66990c1 and committed APPROVE after70.4567s. Source publication bytes are unchanged from the earlier rejection. The Goal is awaiting_confirmation; the human confirmation question was presented, but no consent or completion is assumed. goal-approved-53203.json and goal-browser-53203 preserve the actual proof and visible confirmation control.

The standalone image helper also actually succeeded on the designer's484694-byte source poster with configured Codex, returning literal pixel transcriptions and findings. This is not the receiver editor's inspection: the latter's direct request returned a checkpoint/owner-store integrity failure, now under separate source repair. See official-vision-live-53203.json.

Tools browser acceptance used the actual full-description API, verified absent-path handling and exact clipboard text, keyboard expansion/collapse and complete mobile text. The first mobile check incorrectly compared integer scroll/client heights74/73 with an actual73.28125px box whose overflow is visible; screenshots show all text. The false-positive measurement is retained. General dashboard health is not claimed: the CI preview is not installed UI, WebSockets are deliberately blocked, and Goal screenshots retain an execution-data loading error toast.

Four previously parked Host Gate commands were explicitly reviewed and approved through the admin API without reusable rules. One py_compile replay was independently read from its exact547-byte result blob and exited0; approval of the remaining three is not evidence they executed. See gate-approvals-53203.json.

`direct-continuation-blocker.json` preserves the actual old running-row/terminal-error evidence and the new editor admission rejection after upgrade. PR #35419 (2578d7fec3) is the reviewed source repair; target Test #34689988053 and binary run #34690056205 are pending. It has not repaired the live editor yet.
