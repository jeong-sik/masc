\* Buggy model: legitimate action plus the two leak actions
\* (server-root fallback from the pre-#6542 code, and cross-keeper
\* playground access from the pre-#6580 workspace_default leak).
\* Expected: KeeperWorktreeContainment VIOLATED in one step.
SPECIFICATION SpecBuggy
INVARIANT KeeperWorktreeContainment
INVARIANT TypeOK
CONSTANTS
    Keepers = {v, b, c}
CHECK_DEADLOCK FALSE
