### Fixed

- The TUI approval detail puts every label and value through the terminal
  sanitiser before drawing it. A held call's question carries the command a
  model wrote, and escape sequences in it (cursor up, erase line) reached the
  terminal and could rewrite rows the operator had already read before
  approving. Pane rows can now only be built by the sanitising constructor,
  and the terminal-field guard covers the pane (#38478).
