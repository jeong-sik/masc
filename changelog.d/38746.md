### Fixed

- The `/burn` tab-row cost reads keeper-costs' 24h reading in the Team
  title's words ("at least $N 24h", "cost unknown 24h", "cost not read
  yet") instead of adding up each Keeper's lifetime cost, which took a turn
  with no price as $0 and drew a subscription fleet as `$0.00` (#38746).
