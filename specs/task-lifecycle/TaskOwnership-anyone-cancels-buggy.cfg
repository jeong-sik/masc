\* Cancelling a Task one did not create must be caught.

SPECIFICATION SpecBugAnyoneCancels

CONSTANTS
    Tasks = {t1, t2}
    Agents = {a1, a2}
    MaxSubmissions = 2
    NoOne = NoOne
    Operator = Operator

INVARIANTS
    CancelledRequiresStanding

CHECK_DEADLOCK
    FALSE
