### Documentation

- RFC `official-client-spend-resolved-on-every-attempt` proposes resolving an official client's spend from every attempt of a Keeper turn, failed ones included, recording failed turns in the same meta commit, resolving Codex from its thread count, and naming the counter gaps no client reports (#38993).
