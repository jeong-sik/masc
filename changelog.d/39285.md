### Changed

- Skill resource reads derive their bound from the shared inline tool-result
  boundary. An absent Skill policy still does not enable package sources (#39285).

### Upgrade notes

- `[skills] resource-read-max-bytes` is rejected. Delete that line from
  runtime.toml before upgrading. Save validation and the boot Skill warning
  name the unsupported field and the configuration file (#39285).
